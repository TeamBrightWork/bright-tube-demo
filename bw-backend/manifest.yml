name: BrightTube
models:
  video:
    attributes:
      name: string
      url: string
      type: string
      tags:
        collection: 'tag'
        via: 'videos'
  tag:
    attributes:
      name:
        type: string
        unique: true
      videos:
        collection: video
        via: 'tags'
        dominant: true
